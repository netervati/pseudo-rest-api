name: deploy

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    uses: ./.github/workflows/build.yml

  lint:

    name: lint
    needs: [build]

    steps:
      - name: ESLint and Prettier
        run: npm run lint

